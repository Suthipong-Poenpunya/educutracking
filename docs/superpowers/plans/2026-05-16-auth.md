# Auth System Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** แยก Login/Register เป็น 2 form ใน card เดียว toggle ได้โดยไม่ reload โดย Login ใช้ studentId + ชื่อ exact match

**Architecture:** แก้เฉพาะ `index.html` (HTML structure) และ `app.js` (logic) ตาม Approach A — ไม่สร้างไฟล์ใหม่ ไม่แตะ `Code.gs` หรือ `api.js` เพราะ endpoint และ localStorage fallback ครบอยู่แล้ว toggle ใช้ `display:none/block` บน 2 form element ใน card เดิม

**Tech Stack:** Vanilla JS, HTML, CSS (ไม่มี framework, ไม่มี test runner — verify ด้วย manual browser test)

**Spec:** `docs/superpowers/specs/2026-05-16-auth-design.md`

---

## File Map

| ไฟล์ | การเปลี่ยนแปลง |
|------|---------------|
| `index.html` | แทนที่ `#register-screen` div ด้วย `#auth-screen` card 2 form |
| `app.js` | เพิ่ม `handleLogin()`, `switchAuthMode()` · แก้ `handleRegister()`, `showApp()`, `handleLogout()` |

---

## Task 1: แทนที่ HTML ของ auth screen ใน index.html

**Files:**
- Modify: `index.html` (บรรทัด 27–66 — `<div id="register-screen">...</div>`)

- [ ] **Step 1: แทนที่ block `#register-screen` ทั้งหมด**

ลบ:
```html
  <!-- Registration Screen -->
  <div id="register-screen" class="register-screen">
    <div class="register-card">
      <div style="font-size:48px;margin-bottom:12px">🎓</div>
      <h2>ChulaEduTracker</h2>
      <p class="subtitle">ระบบติดตามและวางแผนการศึกษา<br>ครุศาสตรบัณฑิต สาขาเทคโนโลยีการศึกษา จุฬาฯ</p>
      <form id="register-form" onsubmit="handleRegister(event)" style="text-align:left">
        <div class="form-group">
          <label class="form-label">ชื่อ-นามสกุล</label>
          <input type="text" class="form-input" id="reg-name" required placeholder="เช่น สมชาย ใจดี">
        </div>
        <div class="form-group">
          <label class="form-label">รหัสนิสิต</label>
          <input type="text" class="form-input" id="reg-student-id" required placeholder="เช่น 6438xxxxx27">
        </div>
        <div class="form-group">
          <label class="form-label">โปรแกรม</label>
          <select class="form-select" id="reg-program">
            <option value="single_major">เอกเดี่ยว (เทคโนโลยีการศึกษา)</option>
            <option value="normal">โปรแกรมปกติ</option>
          </select>
        </div>
        <div class="form-row">
          <div class="form-group">
            <label class="form-label">ปีการศึกษาที่เข้า (พ.ศ.)</label>
            <input type="number" class="form-input" id="reg-entry-year" required placeholder="2566" min="2555" max="2580">
          </div>
          <div class="form-group">
            <label class="form-label">ภาคที่เข้า</label>
            <select class="form-select" id="reg-entry-sem">
              <option value="1">ภาคต้น</option>
              <option value="2">ภาคปลาย</option>
            </select>
          </div>
        </div>
        <button type="submit" class="btn btn-primary" style="width:100%;justify-content:center;margin-top:8px">
          เริ่มใช้งาน
        </button>
      </form>
    </div>
  </div>
```

ใส่แทนที่:
```html
  <!-- Auth Screen (Login / Register) -->
  <div id="auth-screen" class="register-screen">
    <div class="register-card">
      <div style="font-size:48px;margin-bottom:12px">🎓</div>
      <h2>ChulaEduTracker</h2>
      <p class="subtitle">ระบบติดตามและวางแผนการศึกษา<br>ครุศาสตรบัณฑิต สาขาเทคโนโลยีการศึกษา จุฬาฯ</p>

      <!-- Login Form -->
      <form id="login-form" onsubmit="handleLogin(event)" style="text-align:left">
        <div class="form-group">
          <label class="form-label">รหัสนิสิต</label>
          <input type="text" class="form-input" id="login-student-id" required placeholder="เช่น 6438xxxxx27" autocomplete="username">
        </div>
        <div class="form-group">
          <label class="form-label">ชื่อ-นามสกุล</label>
          <input type="text" class="form-input" id="login-name" required placeholder="เช่น สมชาย ใจดี" autocomplete="name">
          <p class="auth-hint">⚠️ กรุณาใส่ชื่อตามในบัตรนิสิตด้วยภาษาไทย ไม่มีคำนำหน้า และกรอกชื่อให้ถูกต้อง หากผิด จะไม่สามารถเข้าสู่ระบบได้</p>
        </div>
        <button type="submit" class="btn btn-primary" style="width:100%;justify-content:center;margin-top:8px">เข้าสู่ระบบ</button>
        <p class="auth-switch">ยังไม่มีบัญชี? <a href="#" onclick="switchAuthMode('register');return false;">สมัครใหม่</a></p>
      </form>

      <!-- Register Form (hidden by default) -->
      <form id="register-form" onsubmit="handleRegister(event)" style="text-align:left;display:none">
        <div class="form-group">
          <label class="form-label">ชื่อ-นามสกุล</label>
          <input type="text" class="form-input" id="reg-name" required placeholder="เช่น สมชาย ใจดี" autocomplete="name">
          <p class="auth-hint">⚠️ กรุณาใส่ชื่อตามในบัตรนิสิตด้วยภาษาไทย ไม่มีคำนำหน้า และกรอกชื่อให้ถูกต้อง หากผิด จะไม่สามารถเข้าสู่ระบบได้</p>
        </div>
        <div class="form-group">
          <label class="form-label">รหัสนิสิต</label>
          <input type="text" class="form-input" id="reg-student-id" required placeholder="เช่น 6438xxxxx27" autocomplete="username">
        </div>
        <div class="form-group">
          <label class="form-label">โปรแกรม</label>
          <select class="form-select" id="reg-program">
            <option value="single_major">เอกเดี่ยว (เทคโนโลยีการศึกษา)</option>
            <option value="normal">โปรแกรมปกติ</option>
          </select>
        </div>
        <div class="form-row">
          <div class="form-group">
            <label class="form-label">ปีการศึกษาที่เข้า (พ.ศ.)</label>
            <input type="number" class="form-input" id="reg-entry-year" required placeholder="2566" min="2555" max="2580">
          </div>
          <div class="form-group">
            <label class="form-label">ภาคที่เข้า</label>
            <select class="form-select" id="reg-entry-sem">
              <option value="1">ภาคต้น</option>
              <option value="2">ภาคปลาย</option>
            </select>
          </div>
        </div>
        <button type="submit" class="btn btn-primary" style="width:100%;justify-content:center;margin-top:8px">สมัครใหม่</button>
        <p class="auth-switch">มีบัญชีแล้ว? <a href="#" onclick="switchAuthMode('login');return false;">เข้าสู่ระบบ</a></p>
      </form>
    </div>
  </div>
```

- [ ] **Step 2: เพิ่ม CSS class `.auth-hint` และ `.auth-switch` ใน style.css**

เปิด `style.css` หา section ที่เกี่ยวกับ `.register-card` แล้วเพิ่มต่อท้าย:
```css
.auth-hint {
  font-size: 12px;
  color: var(--warning, #f59e0b);
  margin-top: 6px;
  line-height: 1.5;
}

.auth-switch {
  text-align: center;
  margin-top: 14px;
  font-size: 13px;
  color: var(--text-secondary);
}

.auth-switch a {
  color: var(--primary, #9b1c2e);
  text-decoration: none;
  font-weight: 600;
}

.auth-switch a:hover {
  text-decoration: underline;
}
```

- [ ] **Step 3: ทดสอบ manual — เปิด index.html ในบราวเซอร์**

เปิด `index.html` โดยตรงหรือผ่าน local server ตรวจ:
- เห็น form "เข้าสู่ระบบ" เป็น default
- เห็นคำเตือนสีเหลืองใต้ช่องชื่อ
- กด "สมัครใหม่" → form สลับเป็น register (ยังไม่ทำงานได้เต็มที่ — JS ยังไม่อัพเดท)

---

## Task 2: เพิ่ม switchAuthMode() และแก้ reference ใน app.js

**Files:**
- Modify: `app.js`

- [ ] **Step 1: เพิ่มฟังก์ชัน `switchAuthMode` หลัง `populateDropdowns`**

เปิด `app.js` หาบรรทัด:
```javascript
// ---- Registration ----
```
แล้วเพิ่ม **ก่อน** บรรทัดนั้น:
```javascript
// ---- Auth Mode Toggle ----
function switchAuthMode(mode) {
  const loginForm = document.getElementById('login-form');
  const registerForm = document.getElementById('register-form');
  if (mode === 'register') {
    loginForm.style.display = 'none';
    registerForm.style.display = 'block';
  } else {
    loginForm.style.display = 'block';
    registerForm.style.display = 'none';
  }
  loginForm.reset();
  registerForm.reset();
}

```

- [ ] **Step 2: แก้ `showApp` — เปลี่ยน `register-screen` → `auth-screen`**

หาใน `app.js`:
```javascript
  document.getElementById('register-screen').style.display = 'none';
```
แก้เป็น:
```javascript
  document.getElementById('auth-screen').style.display = 'none';
```

- [ ] **Step 3: แก้ `handleLogout` — เปลี่ยน `register-screen` → `auth-screen`**

หาใน `app.js`:
```javascript
  document.getElementById('register-screen').style.display = 'flex';
```
แก้เป็น:
```javascript
  document.getElementById('auth-screen').style.display = 'flex';
  switchAuthMode('login');
```

(เพิ่ม `switchAuthMode('login')` เพื่อ reset กลับ login form ทุกครั้งที่ logout)

- [ ] **Step 4: ทดสอบ manual**

เปิดบราวเซอร์ ทดสอบ:
- กดลิงก์ "สมัครใหม่" → form สลับ, field ว่างเปล่า
- กดลิงก์ "เข้าสู่ระบบ" → สลับกลับ, field ว่างเปล่า

---

## Task 3: เพิ่ม handleLogin() ใน app.js

**Files:**
- Modify: `app.js`

- [ ] **Step 1: เพิ่มฟังก์ชัน `handleLogin` หลัง `switchAuthMode`**

หาในไฟล์ `app.js` บรรทัด:
```javascript
// ---- Registration ----
async function handleRegister(e) {
```
เพิ่ม **ก่อน** `handleRegister`:
```javascript
// ---- Login ----
async function handleLogin(e) {
  e.preventDefault();
  const studentId = document.getElementById('login-student-id').value.trim();
  const name = document.getElementById('login-name').value.trim();
  if (!studentId || !name) {
    showToast('กรุณากรอกข้อมูลให้ครบถ้วน', 'warning');
    return;
  }

  const res = await API.getUserByStudentId(studentId);
  if (!res.success) {
    showToast('ไม่พบรหัสนิสิตนี้ในระบบ กรุณาสมัครใหม่', 'error');
    return;
  }
  if (res.data.display_name !== name) {
    showToast('ชื่อ-นามสกุลไม่ถูกต้อง กรุณาตรวจสอบและลองใหม่', 'error');
    return;
  }

  currentUser = res.data;
  saveToStorage(STORAGE_KEYS.USER, currentUser);
  showApp();
  showToast('ยินดีต้อนรับ ' + currentUser.display_name + '!', 'success');
}

```

- [ ] **Step 2: ทดสอบ manual — Login flow ทั้ง 3 กรณี**

**กรณี A — รหัสนิสิตไม่มีในระบบ:**
- กรอก studentId ที่ไม่มี, ชื่ออะไรก็ได้ → กด "เข้าสู่ระบบ"
- คาดหวัง: toast แดง "ไม่พบรหัสนิสิตนี้ในระบบ กรุณาสมัครใหม่"

**กรณี B — รหัสถูก แต่ชื่อผิด:**
- สมัครบัญชีด้วยชื่อ "สมชาย ใจดี" ก่อน (Task 4) หรือเพิ่มใน localStorage โดยตรง
- กรอก studentId ถูก, ชื่อ "สมชาย" (ไม่มีนามสกุล) → กด "เข้าสู่ระบบ"
- คาดหวัง: toast แดง "ชื่อ-นามสกุลไม่ถูกต้อง กรุณาตรวจสอบและลองใหม่"

**กรณี C — ถูกทั้งคู่:**
- กรอกข้อมูลตรงกับที่สมัคร → กด "เข้าสู่ระบบ"
- คาดหวัง: เข้าแอปได้, toast เขียว "ยินดีต้อนรับ [ชื่อ]!"

---

## Task 4: แก้ handleRegister() ใน app.js

**Files:**
- Modify: `app.js`

- [ ] **Step 1: แทนที่ `handleRegister` ทั้งฟังก์ชัน**

หาและแทนที่:
```javascript
async function handleRegister(e) {
  e.preventDefault();
  const data = {
    displayName: document.getElementById('reg-name').value.trim(),
    studentId: document.getElementById('reg-student-id').value.trim(),
    program: document.getElementById('reg-program').value,
    entryYear: parseInt(document.getElementById('reg-entry-year').value),
    entrySemester: parseInt(document.getElementById('reg-entry-sem').value)
  };

  // Try to find existing user by studentId (enables cross-device login)
  const existing = await API.getUserByStudentId(data.studentId);
  if (existing.success) {
    currentUser = existing.data;
    saveToStorage(STORAGE_KEYS.USER, currentUser);
    showApp();
    showToast('ยินดีต้อนรับกลับมา ' + currentUser.display_name + '!', 'success');
    return;
  }

  // Create new user if not found
  const res = await API.createUser(data);
  if (res.success) {
    currentUser = { user_id: res.data.user_id, display_name: data.displayName, student_id: data.studentId, program: data.program, entry_year: data.entryYear, entry_semester: data.entrySemester };
    saveToStorage(STORAGE_KEYS.USER, currentUser);
    showApp();
    showToast('ลงทะเบียนสำเร็จ!', 'success');
  }
}
```

ใส่แทนที่:
```javascript
async function handleRegister(e) {
  e.preventDefault();
  const data = {
    displayName: document.getElementById('reg-name').value.trim(),
    studentId: document.getElementById('reg-student-id').value.trim(),
    program: document.getElementById('reg-program').value,
    entryYear: parseInt(document.getElementById('reg-entry-year').value),
    entrySemester: parseInt(document.getElementById('reg-entry-sem').value)
  };

  // ตรวจว่า studentId มีอยู่แล้วหรือไม่
  const existing = await API.getUserByStudentId(data.studentId);
  if (existing.success) {
    showToast('รหัสนิสิตนี้มีผู้ใช้อยู่แล้ว กรุณาเข้าสู่ระบบ', 'error');
    switchAuthMode('login');
    return;
  }

  const res = await API.createUser(data);
  if (res.success) {
    currentUser = {
      user_id: res.data.user_id,
      display_name: data.displayName,
      student_id: data.studentId,
      program: data.program,
      entry_year: data.entryYear,
      entry_semester: data.entrySemester
    };
    saveToStorage(STORAGE_KEYS.USER, currentUser);
    showApp();
    showToast('ลงทะเบียนสำเร็จ!', 'success');
  }
}
```

- [ ] **Step 2: ทดสอบ manual — Register flow ทั้ง 2 กรณี**

**กรณี A — studentId ซ้ำ:**
- สมัครบัญชีหนึ่งก่อน แล้วลอง register ด้วย studentId เดิม
- คาดหวัง: toast แดง "รหัสนิสิตนี้มีผู้ใช้อยู่แล้ว กรุณาเข้าสู่ระบบ" และ switch กลับ login form

**กรณี B — ข้อมูลใหม่:**
- กรอกข้อมูลครบ studentId ใหม่ → กด "สมัครใหม่"
- คาดหวัง: เข้าแอปได้, toast เขียว "ลงทะเบียนสำเร็จ!"

---

## Task 5: Commit

**Files:** `index.html`, `app.js`, `style.css`

- [ ] **Step 1: ตรวจสอบ diff สุดท้าย**

```bash
git diff --stat
```
คาดหวัง: เห็น `app.js`, `index.html`, `style.css` changed

- [ ] **Step 2: ทดสอบ full flow ก่อน commit**

เปิดบราวเซอร์ ทดสอบครบ:
1. หน้าเริ่มต้น → เห็น Login form + คำเตือน
2. กด "สมัครใหม่" → สลับ Register form + คำเตือน
3. สมัคร account ใหม่ → เข้าแอปได้
4. Logout → กลับ Login form (ไม่ใช่ Register)
5. Login ด้วย studentId + ชื่อที่ถูกต้อง → เข้าแอปได้
6. Login ด้วยชื่อผิด → toast error ถูก message

- [ ] **Step 3: Commit**

```bash
git add index.html app.js style.css
git commit -m "feat: แยก Login/Register เป็น toggle card พร้อมคำเตือนการกรอกชื่อ"
```

---

## หมายเหตุสำคัญสำหรับ Agent

- `Code.gs` และ `api.js` **ไม่ต้องแก้** — endpoint `getUserByStudentId` และ localStorage fallback ครบอยู่แล้ว
- `populateDropdowns()` ใน `app.js` ยังทำงานได้เหมือนเดิม เพราะ `#course-grade` และ `#manual-category` ยังอยู่ใน modal (ไม่ได้อยู่ใน auth screen)
- ID ของ register fields (`reg-name`, `reg-student-id`, `reg-program`, `reg-entry-year`, `reg-entry-sem`) ยังเหมือนเดิม — `handleRegister` อ่าน element IDs เหล่านี้อยู่แล้ว ไม่ต้องเปลี่ยน
